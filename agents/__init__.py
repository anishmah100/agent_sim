"""agents — rule-based + LLM baselines used as background population.

Each archetype is an SDK-connected bot. No engine privileges; the only
way they interact with the world is through the same Action verbs an
LLM agent uses.
"""
